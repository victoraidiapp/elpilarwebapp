

var pagess=1;
var paginasavisos=1;
var pagesCurso=1;
var paginascursos=1;
var pagesIntensivo=1;
var paginasIntensivo=1;
var pagesCliente=1;
var paginasCliente=1;

function obtenerImagenes(){
				var data = {
				};
				
		jQuery.getJSON("http://www.autoescuelaselpilar.com/api/imgslideapp", data, function(objjson1) {
		console.log('El servidor me ha dicho: ' + objjson1);
				//$resultado=array('status'=>'ok',imagenes=>array['titulo'=>$titulo,'url'=>$url,];				
					if(objjson1.status=="ok"){
						 var misfotos=objjson1.imagenes;
						 for(foto in misfotos){
							 var imagen='<li><img src="'+misfotos[foto].url+'"></li>';
							 
							jQuery("ul.rslides").append(imagen);					 
						 }
						jQuery(".rslides").responsiveSlides({  auto: true,            
  											speed: 500,            
  											timeout: 4000,
											
											}); 			
					}
		
		});
		
}

	 
	 
    
  	
	
	//obtener imagenes galeria
function imagenesGaleria(){
				var data = {
				};
				
		jQuery.getJSON("http://www.autoescuelaselpilar.com/api/imgaleriaapp", data, function(objjson2) {
		console.log('El servidor me ha dicho: ' + objjson2);
				//$resultado=array('status'=>'ok',imagenesgal=>array['id'=>$idimg,titulo'=>$titulo,'url'=>$url,];
					if(objjson2.status=="ok"){
						 var misfotosgal=objjson2.imagenesgal;
						 for(fotogal in misfotosgal){
							 var imagengal='<div id="'+misfotosgal[fotogal].id+'" class="thumbnail-box"><div class="thumbnail"><a href="'+misfotosgal[fotogal].url+'" title="'+misfotosgal[fotogal].titulo+'" class=""><img  src="'+misfotosgal[fotogal].url+'" width="100" height="75"></a></div></div>';							
							 
							 jQuery("#gallery").append(imagengal);
										 
						 }
										
					}
			
		});
}
	
	// obtener cursos
			
function obtenerCursos() {
			if(pagesCurso<=paginascursos){
				 $.mobile.loading( "show", {
            text: "Cargando cursos",
            textVisible: true,
            theme: "a",
            textonly: false,
            
    });
							var data = {
					type:'wpsc-product',
					count:10,
					page:pagesCurso
				};
				
				
		jQuery.getJSON("http://www.autoescuelaselpilar.com/api/get_products", data, function(objjson5) {
		console.log('El servidor me ha dicho: ' + objjson5);
				 $.mobile.loading("hide");
					if(objjson5.status=="ok"){
						paginascursos=objjson5.pages;
						var miscursos=objjson5.posts;
						for(curso in miscursos){
							jQuery("#cursillos").append(
			'<div class="avis clear">'
			+'<a href="#'+miscursos[curso].id+'" data-rel="popup" class=" popupp" data-transition="pop"><img class="left" width="100px"  src="'+miscursos[curso].thumbnail+'"/></a>'
			+'<div class="tit_curso">'
			+'<a href="#'+miscursos[curso].id+'" data-rel="popup" class=" popupp" data-transition="pop">'+miscursos[curso].title+'</a>'
			+'<div data-role="popup" id="'+miscursos[curso].id+'" class="verpopup"><a href="#" data-rel="back" class="ui-btn ui-corner-all ui-shadow ui-btn-a ui-icon-delete ui-btn-icon-notext ui-btn-right">Close</a><p>'+miscursos[curso].content+'</p></div>'
			+'</div>');
						}
						$('.verpopup').css('overflow-y', 'scroll');
						pagesCurso++;
						$('#miscursillos').trigger('create');
					}
		})
		
			}
		
	}		
		
		//obtener avisos
		
function obtenerAvisos() {
			if(pagess<=paginasavisos){
								 $.mobile.loading( "show", {
            text: "Cargando avisos",
            textVisible: true,
            theme: "a",
            textonly: false,
            
    });
							var data = {
					slug:"avisos",
					count:5,
					page:pagess
				};
				
		jQuery.getJSON("http://www.autoescuelaselpilar.com/api/get_category_posts", data, function(objjson4) {
		console.log('El servidor me ha dicho: ' + objjson4);
								 $.mobile.loading( "hide");
					if(objjson4.status=="ok"){
						paginasavisos=objjson4.pages;
						var misavisos=objjson4.posts;
						for(aviso in misavisos){
							jQuery("#tablon").append(
			'<a href="#'+misavisos[aviso].id+'" data-rel="popup" class=" popupp" data-transition="pop">'
			+'<div class="avis clear">'
			+'<img class="left" width="100px"  src="'+misavisos[aviso].thumbnail+'"/>'
			+'<div class="tit_aviso">'+misavisos[aviso].title+'</div><div class="content_aviso">'+misavisos[aviso].excerpt +'</div></a>'
			+'<div data-role="popup" id="'+misavisos[aviso].id+'" class="verpopup"><a href="#" data-rel="back" class="ui-btn ui-corner-all ui-shadow ui-btn-a ui-icon-delete ui-btn-icon-notext ui-btn-right">Close</a><p class="no_enlace">'+misavisos[aviso].content+'</p></div>'
			+'</div>');
						}
						$('.verpopup').css('overflow-y', 'scroll'); 
						
						pagess++;
						$('#avisillos').trigger('create');
					}
		})
		
			}
		
	}					                
	
	//obtener intensivos
	
	function obtenerIntensivos() {
			if(pagesIntensivo<=paginasIntensivo){
				 $.mobile.loading( "show", {
            text: "Cargando cursos intensivos",
            textVisible: true,
            theme: "a",
            textonly: false,
            
    });
							var data = {
					type:'wpsc-product',
					count:10,
					page:pagesIntensivo
				};
				
				
		jQuery.getJSON("http://www.autoescuelaselpilar.com/api/get_products", data, function(objjson6) {
		console.log('El servidor me ha dicho: ' + objjson6);
				 $.mobile.loading("hide");
					if(objjson6.status=="ok"){
						paginasIntensivo=objjson6.pages;
						console.log("he llegado");
						var misintensivos=objjson6.posts;
						for(intensivo in misintensivos){
							console.log("he llegado");
							jQuery("#intensivillos").append(
							
			'<div class="avis clear">'
			+'<a href="#'+misintensivos[intensivo].id+'" data-rel="popup" class=" popupp" data-transition="pop"><img class="left" width="100px"  src="'+misintensivos[intensivo].thumbnail+'"/></a>'
			+'<div class="tit_curso">'
			+'<a href="#'+misintensivos[intensivo].id+'" data-rel="popup" class=" popupp" data-transition="pop">'+misintensivos[intensivo].title+'</a>'
			+'<div data-role="popup" id="'+misintensivos[intensivo].id+'" class="verpopup"><a href="#" data-rel="back" class="ui-btn ui-corner-all ui-shadow ui-btn-a ui-icon-delete ui-btn-icon-notext ui-btn-right">Close</a><p>'+misintensivos[intensivo].content+'</p></div>'
			+'</div>');
						}
						
						$('.verpopup').css('overflow-y', 'scroll');
						pagesIntensivo++;
						
						$('#misintensivo').trigger('create');
					}
		})
		
			}
		
	}		
			
// obtener clientes
			
function obtenerClientes() {
			if(pagesCliente<=paginasCliente){
				 $.mobile.loading( "show", {
            text: "Cargando clientes",
            textVisible: true,
            theme: "a",
            textonly: false,
            
    });
							var data = {
					type:'wpsc-product',
					count:10,
					page:pagesCliente
				};
				
				
		jQuery.getJSON("http://www.autoescuelaselpilar.com/api/get_products", data, function(objjson7) {
		console.log('El servidor me ha dicho: ' + objjson7);
				 $.mobile.loading("hide");
					if(objjson7.status=="ok"){
						paginasCliente=objjson7.pages;
						var misclientes=objjson7.posts;
						for(cliente in misclientes){
							jQuery("#clientillos").append(
			'<div class="avis clear">'
			+'<div class="tit_curso">'
			+'<a href="#'+misclientes[cliente].id+'" data-rel="popup" class=" popupp" data-transition="pop">'+misclientes[cliente].title+'</a>'
			+'<div data-role="popup" id="'+misclientes[cliente].id+'" class="verpopup"><a href="#" data-rel="back" class="ui-btn ui-corner-all ui-shadow ui-btn-a ui-icon-delete ui-btn-icon-notext ui-btn-right">Close</a><p>'+misclientes[cliente].content+'</p></div>'
			+'</div>');
						}
						$('.verpopup').css('overflow-y', 'scroll');
						pagesCliente++;
						$('#misclientillos').trigger('create');
					}
		})
		
			}
		
	}		
	
	
	//boton enviar contacto
jQuery("#envio").click(function(){
		var nombre=jQuery("#nombre").val();
		var email=jQuery("#email").val();
		var tema=jQuery("#tema").val();
		var mensaje=jQuery("#mensaje").val();
		console.log(nombre+email+tema+mensaje);
		var data = {
			nombre: nombre,
			email: email,
			tema:tema,
			mensaje:mensaje
		};
		
		console.log(nombre+email+tema+mensaje);
		
	jQuery.post( "www.autoescuelaselpilar.com/api/dudasfromapp", data, function(response) {
		console.log('El servidor me ha dicho: ' + response);
			if(response.status=="error"){
				alert(response.error);
			}else{
				alert(response.mensaje);
			}
			})
});


	 
